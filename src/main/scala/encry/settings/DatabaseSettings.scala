package encry.settings

case class DatabaseSettings(host: String,
                            user: String,
                            password: String,
                            maxPoolSize: Int)
